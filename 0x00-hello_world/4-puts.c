#include <stdio.h>

/**
 * the program loads the main function
 * thats the starting point.
 *
 * ends the execution by returning a 
 * value. return(0)
 *
 */
int main (void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
